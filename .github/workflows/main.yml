python <<EOF
          import os
          import requests
          import re

          def get_token():
              uid = os.getenv('FT_UID')
              secret = os.getenv('FT_SECRET')
              response = requests.post('https://api.intra.42.fr/oauth/token', data={
                  'grant_type': 'client_credentials',
                  'client_id': uid,
                  'client_secret': secret
              })
              return response.json()['access_token']

          def get_user_data(token):
              headers = {'Authorization': f'Bearer {token}'}
              response = requests.get('https://api.intra.42.fr/v2/users/sunwkim', headers=headers)
              return response.json()

          token = get_token()
          data = get_user_data(token)

          # 42 cursuses 중 Common Core(id: 21) 정보를 우선적으로 찾습니다.
          # 만약 없으면 가장 마지막 cursus 정보를 가져옵니다.
          cursus_info = next((c for c in data.get('cursus_users', []) if c.get('cursus_id') == 21), data.get('cursus_users', [{}])[-1])

          level = cursus_info.get('level', '0.00')
          blackhole = cursus_info.get('blackhole_expiration_date', 'N/A')
          if blackhole and blackhole != 'N/A':
              blackhole = blackhole.split('T')[0] # 날짜만 깔끔하게 출력
          
          # 코알리숑 정보 안전하게 가져오기
          coalition = "N/A"
          # 별도의 API 호출이 필요할 수 있으나 기본 정보에서 시도
          if data.get('groups'):
              coalition = data['groups'][0].get('name', 'N/A')

          stats_text = f"### 📊 42 Stats (sunwkim)\n- **Level:** {level}\n- **Blackhole:** {blackhole}\n- **Campus:** 42 Lausanne\n"

          with open('README.md', 'r', encoding='utf-8') as f:
              content = f.read()

          # 정규표현식을 사용하여 주석 사이의 내용 교체
          new_content = re.sub(r'.*?', 
                               f'\n{stats_text}\n', 
                               content, flags=re.DOTALL)

          with open('README.md', 'w', encoding='utf-8') as f:
              f.write(new_content)
          EOF
